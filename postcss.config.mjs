/**
 * PostCSS Configuration
 *
 * Tailwind CSS v4 (via @tailwindcss/vite) automatically sets up all required
 * PostCSS plugins — this file only exists for adding additional PostCSS plugins
 * if needed. The original Make left it empty.
 */
export default {}
